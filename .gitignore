# Python caches only. Do NOT ignore queue/, applied/, state/ or *.pdf: in a
# private copy the repo IS the database, so those must be committed.
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/

# queue/raw/ is the optional sweep's fetch-time scratch, never durable state.
queue/raw/
